{
  // Global Zed settings using mlt lsp-auto
  // Place this in: ~/.config/zed/settings.json (Linux) or ~/Library/Application Support/Zed/settings.json (macOS)
  //
  // How it works:
  // - Local projects (no docker-compose.yml): Direct passthrough to ruff/basedpyright (~0ms overhead)
  // - Remote projects (has docker-compose.yml): Uses mlt lsp-proxy with path translation
  //
  // This single config works everywhere - no per-project settings needed!

  "lsp": {
    "ruff": {
      "binary": {
        "path": "mlt",
        "arguments": ["lsp-auto", "ruff", "server"]
      }
    },
    "basedpyright": {
      "binary": {
        "path": "mlt",
        "arguments": ["lsp-auto", "basedpyright-langserver", "--stdio"]
      }
    }
  },

  // Optional: Additional Zed settings
  "theme": "One Dark",
  "buffer_font_size": 14,
  "vim_mode": true,
  "format_on_save": "on",
  "formatter": "language_server",
  "tab_size": 4,
  "soft_wrap": "none"
}
